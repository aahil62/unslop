name: Canonical Drift Check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  canonical-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run canonical check
        run: bash scripts/check-canonical.sh
